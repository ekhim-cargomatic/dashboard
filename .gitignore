node_modules/
dist/
*.log
.DS_Store
.vite/

# Local fixture data — regenerate with `npm run fixtures`
public/runs/
public/config.json

# Untracked: embeds the AWS account id, and this repo is public
infra/deploy.env

# Untracked: the Notion integration secret, read by infra/deploy-notion-fn.sh.
# Never commit — it grants write access to whatever the integration is shared with.
infra/.notion-token

# Untracked: internal training matrix (named owners, internal doc links) — repo is public
data/

# Generated Atlas output — feature names and ticket ids; repo is public
atlas.json
atlas.html
